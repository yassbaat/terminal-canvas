# Naming & Domains

Checked via whois (Verisign) on 2026-07-27. `.com` results are authoritative; re-verify `.app`/`.dev` at a registrar (whois for Google-registry TLDs didn't resolve from this machine — DNS checks below are a strong hint, not proof).

## Verified AVAILABLE (.com)

| Name | Domain | Why it works |
|------|--------|--------------|
| **Termloom** | termloom.com | Weaving many threads (terminals/agents) into one fabric. Short, brandable, pronounceable. **Top pick.** |
| **Termflock** | termflock.com | A flock of agents — perfectly on-theme for multi-agent work. Most unique/ownable. |
| **Termstage** | termstage.com | "Stage" doubles as the Focus Mode metaphor. Clean and professional. |
| **Shellcanvas** | shellcanvas.com | Fully descriptive — says exactly what it is. Best for SEO, least brandable. |
| **Canvasterm** | canvasterm.com | Descriptive variant, reads slightly awkward. |
| **Terminal Canvas** | terminalcanvas.com | The current name. Domain is free — buy it regardless as a redirect. |

DNS checks also suggest `termloom.dev`, `shellcanvas.app`, `termstage.app` are unregistered. `termloom.app` has DNS — likely registered.

## Verified TAKEN (.com)
termdeck, termhive, termscape, termcanvas, muxboard, termboard, agentmux, swarmterm, termgrid, hiveterm, termfield, paneless, termweave, agentcanvas.

## Recommendation

1. **Brand: Termloom** — tagline: *"The infinite canvas for your coding agents."*
   - Caveat to be aware of: contains the word "loom" (Loom the video company). Different category, so low practical risk, but if you want zero adjacency, **Termflock** is the most ownable name on the list.
2. **Buy today** (~$25 total, before someone else does): `termloom.com` + `terminalcanvas.com` (redirect — it's your current product name and it's ungoogleably generic, which is exactly why you shouldn't keep it as the brand).
3. All launch copy in `reddit-launch.md` and `gumroad-listing.md` uses **Termloom**. If you pick differently, find-and-replace.
